# API Contracts

This document defines every API used by the frontend, backend and AI.

Every team member must follow this document.

No API should be changed without updating this document.

Example

POST /auth/login

POST /auth/register

POST /petitions

GET /petitions/{id}

GET /dashboard

POST /ai/analyze

POST /ai/search

GET /analytics

GET /notifications